# reader-demo

## Python 2.7

Run `python -m SimpleHTTPServer 8000` and browse to http://localhost:8000/reader.html

## Python 3.4

Run `python -m http.server` and browse to http://localhost:8000/reader.html
